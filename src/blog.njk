---
layout: body
markdownFile: true
title: Blog
pagination:
  data: collections.blogPosts
  size: 9
  reverse: true
  alias: blogPosts
---

<h1 class='inline-block mr-4'>Evently Blog</h1>
<a href='/tags' class='inline-block'>view all tags</a>
<ol class='blog-list grid gap-5 lg:grid-cols-3 md:grid-cols-2 sm:grid-cols-1'>
  {% for post in blogPosts %}
    {% include 'partials/blog-list-post.njk' %}
  {% endfor %}
</ol>

<nav aria-label='More blog posts' class='blog-pagination my-10'>
  <ol class='flex content-between'>
    <div class='text-left w-1/2'>
      <li>
        {% if page.url != pagination.href.first %}
          <a href='{{ pagination.href.first }}' class='mr-2'>
            First
          </a>
        {% endif %}
      </li>
      <li>
        {% if pagination.href.previous %}
          <a href='{{ pagination.href.previous }}'>
            Previous
          </a>
        {% endif %}
      </li>
    </div>
    <div class='text-right w-1/2'>
      <li>
        {% if pagination.href.next %}
          <a href='{{ pagination.href.next }}'>
            Next
          </a>
        {% endif %}
      </li>
      <li>
        {% if page.url != pagination.href.last %}
          <a href='{{ pagination.href.last }}' class='ml-2'>
            Last
          </a>
        {% endif %}
      </li>
    </div>
  </ol>
</nav>

<div class='blog-newsletter text-center bg-gray-100 rounded-md py-10'>
  <p>Keep up to date with Evently by signing up for our newsletter with updates straight to your inbox.</p>
  {% include 'partials/newsletter-form.njk' %}
</div>
