---
site-title: Evently Cloud
js:
fontAwesomeKitID: ea7561e5ef
googleFontsURL: <style> @import url('https://fonts.googleapis.com/css2?family=Assistant&family=Baloo+2&display=swap'); </style>
wip: false
logo: logo-4
---
<!DOCTYPE html>
<html lang="en">
    <head>
        {# Meta #}
        <title>{{ siteTitle }}</title>
        <meta charset="UTF-8"/>
        {% if siteDescription %}
            <meta name="description" content="{{ siteDescription }}"/>
        {% endif %}
        <meta http-equiv="x-ua-compatible" content="ie=edge"/>
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no, viewport-fit=cover"/>

        {# Style #}
        <link rel="stylesheet" href="/css/main.css"/>
        <link rel="stylesheet" href="/css/prism.css"/>
        <link rel="shortcut icon" href="/assets/favicon.ico"/>
        <script src="https://kit.fontawesome.com/{{ fontAwesomeKitID }}.js" crossorigin="anonymous"></script>
        {{ googleFontsURL | safe }}

        {# Functional #}
        {% if js %}
            <script src="/{{ js }}"></script>
        {% endif %}
        <script defer src="https://unpkg.com/alpinejs@3.x.x/dist/cdn.min.js"></script>
        <script src="https://cdn.jsdelivr.net/npm/particles.js@2.0.0/particles.min.js"></script>

    </head>
    <body>
        {% if wip %}
            <div class="overflow-hidden absolute transform -rotate-45 bg-gray-300 text-gray-600 top-9 z-0 left-12 text-sm w-56 -translate-x-1/2 p-x text-center origin-center" style="z-index: -10;">Work in Progress</div>
        {% endif %}

        {% block content %}
            {{ content | safe }}
        {% endblock %}
    </body>
</html>
